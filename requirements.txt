# Default quick-run environment for the public notebooks.
# This includes a CPU-safe PyTorch install; replace it with the
# official PyTorch command from https://pytorch.org/get-started/locally/
# if you want a CUDA-specific build.

torch>=2.0
numpy>=1.20
PyYAML>=6.0
matplotlib>=3.5
seaborn>=0.12
mdtraj>=1.9.9
openmm>=8.0
pymbar>=4.0
jupyterlab>=4.0
ipykernel>=6.0
